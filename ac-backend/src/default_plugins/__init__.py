"""
Package containing all default plugins.
Each plugin should be a package.

Each plugin should be a directory with two files:
- __init__.py
- plugin.py
- [optional] resources

plugin.py should contain a class implementing the CatalogPlugin abstract class and the corresponding identifier.
resources is a directory containing any additional files required by the plugin
"""
